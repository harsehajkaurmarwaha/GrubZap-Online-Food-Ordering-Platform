import React from 'react';

const TooltipProvider = ({ children }: { children: React.ReactNode }) => {
  // Simple placeholder tooltip provider component
  return <>{children}</>;
};

export { TooltipProvider };
